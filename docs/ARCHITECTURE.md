# 🏗️ Архитектура: Robo-Restorer (AI Conservator)

Этот документ описывает высокоуровневую архитектуру системы **Robo-Restorer**, объясняя взаимодействие бэкенда, фронтенда и компонентов ИИ.

## 1. Обзор системы

**Robo-Restorer v2.0.4 LAB** — это гибридная система AI-IoT, разработанная для помощи в сохранении артефактов. Она объединяет данные датчиков в реальном времени с анализом компьютерного зрения для предоставления практических рекомендаций.

### Ключевые особенности архитектуры

- **Модульность:** Четкое разделение между frontend, backend, AI и сенсорами
- **Масштабируемость:** FastAPI обеспечивает высокую производительность и поддержку асинхронности
- **Гибкость:** Поддержка как эмулированных, так и реальных датчиков Arduino
- **Расширяемость:** Легкая интеграция новых моделей и типов датчиков
- **Современный UI:** Cyber-Lab дизайн с темной/светлой темой

### Основные компоненты

1.  **Frontend (UI Layer)**:
    -   **Технологии**: React, Vite, TypeScript.
    -   **Роль**: Пользовательский интерфейс для загрузки изображений, просмотра данных датчиков и чтения отчетов ИИ.
    -   **Стиль**: Эстетика "Cyber-Lab" (Glassmorphism, элементы HUD).

2.  **Backend (Service Layer)**:
    -   **Технологии**: Python, FastAPI, Uvicorn.
    -   **Роль**: Оркестрация потоков данных, управление API и обработка бизнес-логики.

3.  **AI Engine (Intelligence Layer)**:
    -   **Зрение**: TensorFlow / Teachable Machine (Локальный инференс).
    -   **LLM**: OpenAI GPT-4o (Облачный инференс через API).
    -   **Роль**: Классификация повреждений артефактов и генерация подробных отчетов по реставрации.

4.  **Sensors (Hardware Layer)**:
    -   **Технологии**: Arduino (DHT22, LDR) или программный эмулятор.
    -   **Роль**: Предоставление контекста окружающей среды (Температура, Влажность, Свет).

---

## 2. Диаграмма потока данных

```mermaid
graph TD
    User[Пользователь / Реставратор] -->|Загружает изображение| UI[Frontend (React)]
    UI -->|POST /api/analyze| API[Backend (FastAPI)]
    
    subgraph Backend Logic
        API -->|1. Чтение датчиков| Sensors[Модуль датчиков]
        Sensors -.->|Serial| Arduino[Arduino Hardware]
        Sensors -.->|Mock Data| Emulator[Программный эмулятор]
        
        API -->|2. Анализ изображения| TF[Модель TensorFlow]
        TF -->|Класс и уверенность| Core[Decision Engine]
        
        Core -->|3. Правила и пороги| Logic[Оценка риска]
        
        Logic -->|4. Генерация контекста| LLM_Client[Клиент OpenAI]
        LLM_Client -->|Промпт + Данные| OpenAI[OpenAI API]
        OpenAI -->|AI Отчет| LLM_Client
    end
    
    API -->|JSON Ответ| UI
    UI -->|Отображение результатов| User
```

---

## 3. Детали компонентов

### Структура бэкенда (`backend/`)

-   **`web_main.py`**: Точка входа. Запускает сервер Uvicorn.
-   **`ui/web_app.py`**: Определяет маршруты FastAPI (`/api/analyze`). Обрабатывает CORS и загрузку файлов.
-   **`ai/`**:
    -   `model_loader.py`: Синглтон для загрузки SavedModel TensorFlow.
    -   `inference.py`: Предобработка изображений и запуск предсказания.
-   **`sensors/`**:
    -   `serial_reader.py`: Читает данные с физического Arduino через USB.
    -   `emulator.py`: Генерирует реалистичные фейковые данные для тестов/демо.
-   **`core/`**:
    -   `decision_engine.py`: Объединяет правила датчиков + класс зрения для определения логики риска.
    -   `schemas.py`: Модели Pydantic для типизации.

### Структура фронтенда (`frontend/`)

-   **`App.tsx`**: Основная логика приложения. Обрабатывает выбор файлов, вызовы API и управление состоянием.
-   **`index.css`**: Содержит дизайн-систему "Cyber-Lab" (CSS переменные, анимации, Glassmorphism).
-   **`components/`** (Концептуально):
    -   Дисплей HUD для датчиков.
    -   Кнопки и поля ввода в кибер-стиле.

---

## 4. Ключевые технологии и решения

### Backend Stack
-   **FastAPI**: Выбран за высокую производительность, автоматическую генерацию документации OpenAPI (Swagger UI) и поддержку асинхронности
-   **TensorFlow SavedModel**: Обеспечивает совместимость с моделями, экспортированными из Google Teachable Machine
-   **Интеграция OpenAI**: Использует библиотеку Python `openai` для генерации читаемых, контекстно-зависимых отчетов на основе сырых данных
-   **Uvicorn**: ASGI сервер для высокопроизводительного обслуживания API

### Frontend Stack
-   **React 19**: Последняя версия с улучшенной производительностью
-   **Vite 7**: Обеспечивает мгновенную перезагрузку модулей (HMR) и быструю сборку
-   **TypeScript**: Статическая типизация для надежности кода
-   **Modern CSS**: Использование CSS переменных, Glassmorphism, анимаций

### Архитектурные решения

**Разделение ответственности:**
- Frontend отвечает только за отображение и взаимодействие с пользователем
- Backend обрабатывает всю бизнес-логику и координацию между компонентами
- AI модули изолированы и легко заменяемы

**Обработка данных:**
- Датчики могут работать в режиме эмулятора или с реальным Arduino
- Значения сенсоров округляются до 2 знаков после запятой на уровне отображения
- Все данные проходят валидацию через Pydantic схемы

**Производительность:**
- TensorFlow модель загружается один раз при старте (Singleton pattern)
- Изображения предобрабатываются перед инференсом
- Frontend использует React hooks для оптимизации рендеринга

---

## 5. Потоки данных (подробно)

### 1. Инициализация системы
```
1. Запуск Backend → Загрузка TensorFlow модели → Инициализация OpenAI клиента
2. Запуск Frontend → Подключение к Backend API
3. Проверка доступности датчиков (Arduino или Emulator)
```

### 2. Обработка запроса анализа
```
User Action → Upload Image
  ↓
Frontend → POST /api/analyze (multipart/form-data)
  ↓
Backend → Сохранение временного файла
  ↓
Параллельно:
  - Чтение датчиков (Serial/Emulator)
  - Анализ изображения (TensorFlow)
  ↓
Decision Engine → Объединение результатов
  ↓
OpenAI Client → Генерация отчета
  ↓
Backend → Формирование JSON ответа
  ↓
Frontend → Отображение результатов с округлением чисел
```

### 3. Обработка ошибок
- Валидация файла на стороне backend
- Обработка timeout для OpenAI API
- Fallback на эмулятор при недоступности Arduino
- Понятные сообщения об ошибках для пользователя

---

## 6. Безопасность

- **CORS**: Настроен для разрешения запросов только с frontend домена
- **Валидация файлов**: Проверка типа и размера загружаемых изображений
- **Переменные окружения**: Чувствительные данные (API ключи) хранятся в `.env`
- **Обработка ошибок**: Детали внутренних ошибок не раскрываются клиенту

---

## 7. Масштабируемость и будущие улучшения

### Возможности расширения:
1. **Поддержка нескольких моделей** — выбор модели в зависимости от типа артефакта
2. **База данных** — хранение истории анализов и артефактов
3. **Аутентификация** — многопользовательский режим с ролями
4. **Реального времени мониторинг** — WebSocket для постоянного отслеживания датчиков
5. **Облачное развертывание** — Docker контейнеризация для deployment
6. **Мобильное приложение** — React Native версия
7. **Расширенная аналитика** — Dashboard с графиками и трендами

### Текущие ограничения:
- Однопользовательский режим
- Отсутствие персистентного хранилища
- Зависимость от OpenAI API для полнофункционального отчета
- Ограниченная масштабируемость без контейнеризации
