# 🛠️ Руководство по установке и настройке

Это руководство проведет вас через процесс установки и настройки проекта **Robo-Restorer** на вашем локальном компьютере.

## Предварительные требования

- **Python**: Версия 3.10 или выше (3.11 рекомендуется).
- **Node.js**: Версия 18+ (тестировалось с v22.15).
- **Git**: Для клонирования репозитория.
- **Операционная система**: macOS (Apple Silicon рекомендуется для производительности TensorFlow), Linux или Windows (WSL2 рекомендуется).

---

## 1. Клонирование репозитория

```bash
git clone https://github.com/Andasbek/robo-restorer.git
cd robo-restorer
```

---

## 2. Настройка Backend (Python)

### 2.1. Создание виртуального окружения

Рекомендуется использовать виртуальное окружение для управления зависимостями.

```bash
# Создать окружение
python3 -m venv venv

# Активировать его (macOS/Linux)
source venv/bin/activate

# Windows (PowerShell)
# .\venv\Scripts\activate
```

### 2.2. Установка зависимостей

Установите необходимые пакеты Python из `requirements.txt`:

```bash
pip install -r requirements.txt
```

> **Примечание для TensorFlow на macOS:** Если возникнут проблемы, возможно, потребуется установить специальные пакеты:
> `pip install tensorflow-macos tensorflow-metal`

### 2.3. Конфигурация переменных окружения

1.  Создайте файл `.env` в корне проекта:
    ```bash
    touch .env
    ```
2.  Добавьте ваш ключ OpenAI API в этот файл:
    ```env
    OPENAI_API_KEY=sk-proj-ВАШ_КЛЮЧ_ЗДЕСЬ
    ```

**Важно:** Никогда не коммитьте ваш файл `.env` в систему контроля версий! Он игнорируется через `.gitignore`.

---

## 3. Настройка Frontend (React/Vite)

### 3.1. Переход в директорию Frontend

```bash
cd frontend
```

### 3.2. Установка зависимостей

Используя `npm`:

```bash
npm install
```

---

## 4. Запуск приложения

Для полноценной работы нужно запустить оба сервера (backend и frontend) одновременно.

### 4.1. Запуск Backend API

Откройте терминал в корне проекта:

```bash
# Убедитесь, что venv активен
source venv/bin/activate

# Запуск сервера Uvicorn
python -m backend.web_main
```

API должен запуститься по адресу `http://localhost:8000`.

### 4.2. Запуск Frontend UI

Откройте **отдельное** окно терминала в `frontend/`:

```bash
cd frontend
npm run dev
```

Веб-интерфейс будет доступен по адресу `http://localhost:5173`.

---

## 5. Устранение неполадок

-   **"ModuleNotFoundError: No module named 'openai'"**: Запустите `pip install openai`.
-   **"Failed to fetch" ошибка в UI**: Убедитесь, что бэкенд сервер работает на порту 8000.
-   **OpenAI API Error 401 (Unauthorized)**: Ваш API ключ неверный или не загружен.
    1.  Проверьте `.env` на опечатки.
    2.  **Перезапустите бэкенд сервер** (Ctrl+C, затем снова запустите), чтобы перезагрузить переменные окружения.
