# 🚀 Инструкция по запуску проекта **AI Conservator / Robo-Restorer**

Проект состоит из двух частей:

1. **Backend (FastAPI + TensorFlow + OpenAI)** — анализ изображения, датчики, правила, AI-отчёт.
2. **Frontend (Vite + React)** — красивый веб-интерфейс для демонстрации.

---

## 1. Предварительные требования

**ОС:** macOS (M1/M2 подойдёт)
**Установлено:**

* Python 3.11 (подойдёт и 3.10, если всё собиралось под ним)
* Node.js + npm (лучше LTS-версия)
* Git (по желанию)

---

## 2. Структура проекта (основное)

```bash
robo-restorer/
  backend/
    config.py
    web_main.py
    ui/
      web_app.py
      cli_app.py
    ai/
      inference.py
      model_loader.py
    core/
      decision_engine.py
      schemas.py
    sensors/
      emulator.py
      env_status.py
      serial_reader.py
  data/
    images/
      test_artifact.jpg      # пример картинки
    models/
      tm_savedmodel/         # Teachable Machine SavedModel
        assets/
        saved_model.pb
        variables/
      keras_model.h5         # (не используется, можно оставить)
      labels.txt             # классы модели (0 corrosion, 1 crack, ...)
  frontend/
    src/App.tsx
    src/index.css
    ...
  venv/                      # виртуальное окружение Python
  .env                       # OpenAI ключ
```

---

## 3. Настройка backend

### 3.1. Создать и активировать виртуальное окружение

Из корня проекта:

```bash
cd robo-restorer

python -m venv venv
source venv/bin/activate  # на macOS / Linux
# .\venv\Scripts\activate  # на Windows, если вдруг
```

### 3.2. Установить зависимости backend

(Если у тебя уже есть `requirements.txt` — используй его. Если нет — ориентировочно:)

```bash
pip install fastapi uvicorn[standard] tensorflow-macos opencv-python openai pyserial
```

* то, что ты уже ставил (если есть отдельный `requirements.txt`):

```bash
pip install -r requirements.txt
```

---

## 4. Файлы модели (Teachable Machine)

Убедись, что у тебя есть:

```bash
data/models/tm_savedmodel/
  assets/
  saved_model.pb
  variables/
data/models/labels.txt
```

Это экспорт из **Teachable Machine** в формате **TensorFlow SavedModel** + `labels.txt`.

Если всё на месте — дальше ничего делать не нужно.

---

## 5. Файл `.env` и ключ OpenAI

В корне проекта `robo-restorer/` создай файл `.env`:

```env
OPENAI_API_KEY=sk-ВАШ_КЛЮЧ_ИЗ_OPENAI
```

Проверь, что бэкенд его видит:

```bash
(venv) python -c "from backend import config; print('KEY =', repr(config.OPENAI_API_KEY))"
```

Ожидаемый результат: `KEY = 'sk-...'`, а не `None`.

---

## 6. Проверка модели (test.py)

Если у тебя есть `test.py` в корне проекта:

```python
from pathlib import Path
from backend.ai.inference import analyze_image

if __name__ == "__main__":
    img_path = Path("data/images/test_artifact.jpg")
    print("Проверяю модель на:", img_path)

    result = analyze_image(str(img_path))
    print("Результат анализа:")
    print(result)
```

Запуск:

```bash
(venv) python test.py
```

Должен показать что-то вроде:

```text
Проверяю модель на: data/images/test_artifact.jpg
Результат анализа:
{'image_class': '0 corrosion', 'probabilities': {...}, 'confidence': 0.87}
```

Если это так — модель готова.

---

## 7. Запуск backend (API)

Бэкенд запускается через `uvicorn`:

```bash
(venv) python -m backend.web_main
```

По умолчанию он слушает `http://localhost:8000`.

Проверь в браузере:
зайди на `http://localhost:8000` — должна открыться простая страница с надписью, что это AI Conservator API.

---

## 8. Настройка и запуск frontend (Vite + React)

### 8.1. Установка зависимостей фронтенда

В другом терминале:

```bash
cd robo-restorer/frontend
npm install
```

### 8.2. Запуск фронта

```bash
npm run dev
```

По умолчанию Vite запускает фронт на `http://localhost:5173`.

---

## 9. Как пользоваться web-интерфейсом

1. Убедись, что **backend** запущен:
   `http://localhost:8000` открывается.
2. Убедись, что **frontend** запущен:
   `http://localhost:5173` открывается.

Дальше:

1. Открой `http://localhost:5173` в браузере.
2. В блоке **«Анализ артефакта»**:

   * Выбери файл изображения (`test_artifact.jpg` или свой снимок фрагмента).
   * Оставь галочку «Использовать эмулятор» (для демонстрации без Arduino).
   * Выбери язык AI-отчёта (RU / EN).
   * Нажми **«Запустить анализ»**.
3. Справа появится:

   * Блок **Показания датчиков** (температура, влажность, свет, статус среды).
   * Блок **Поверхность артефакта** (класс модели, уверенность, распределение).
   * Блок **Итоговое заключение (правила)** — уровень риска + рекомендация.
   * Блок **AI-отчёт (OpenAI)** — красиво сформулированный текст.

---

## 10. Запуск CLI-версии (консольный режим) — по желанию

У тебя есть файл `backend/main.py` с запуском CLI.

Пример:

```bash
(venv) python -m backend.main --image data/images/test_artifact.jpg --emulator
```

* `--image` — путь к изображению.
* `--emulator` — использовать эмулятор датчиков (без Arduino).

В консоль выведутся:

* показания датчиков,
* результат модели,
* rule-based заключение,
* текст отчёта OpenAI.

---

## 11. Подключение реального Arduino (опционально)

Когда захочешь заменить эмулятор на Arduino:

1. В `backend/sensors/serial_reader.py` проверь `SERIAL_PORT` (для macOS это что-то вроде `/dev/cu.usbmodem...`).
2. В вебе/CLI **убери галочку/флаг эмулятора**:

   * CLI: не указывай `--emulator`,
   * Web API: отправляй запрос без `use_emulator` (или снимай чекбокс на форме, если сделаешь такую опцию).
3. Убедись, что плата подключена и выдаёт данные в формате, который ожидает `SerialReader`.

---

## 12. Частые проблемы и решения

* **`Failed to fetch` на фронте**
  → Проверь, что:

  * backend запущен (`http://localhost:8000` работает);
  * в `web_app.py` добавлен CORS для `http://localhost:5173`.

* **`OPENAI_API_KEY не найден`**
  → Проверь:

  * файл `.env` в корне проекта,
  * что в `config.py` ключ читается (как в примере выше),
  * результат команды:

    ```bash
    (venv) python -c "from backend import config; print(config.OPENAI_API_KEY)"
    ```

* **Ошибки TensorFlow/модели**
  → Убедись, что:

  * модель — именно SavedModel (`tm_savedmodel/`),
  * `labels.txt` лежит рядом,
  * `SAVEDMODEL_DIR` и `LABELS_PATH` правильно указаны в `config.py`.

