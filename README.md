# 🚀 Robo-Restorer (AI Conservator)

> **Интеллектуальная система для реставрации артефактов с использованием AI + IoT**
> Версия: **v2.0.4 LAB**

Robo-Restorer — это гибридная AI-IoT система, объединяющая данные датчиков в реальном времени с анализом компьютерного зрения для помощи в сохранении и реставрации артефактов.

## ✨ Основные возможности

- 🎨 **Анализ повреждений** — классификация коррозии, трещин и других дефектов с помощью TensorFlow
- 🌡️ **Мониторинг среды** — отслеживание температуры, влажности и освещенности
- 🤖 **AI-отчеты** — генерация подробных рекомендаций через OpenAI GPT-4
- 💻 **Современный интерфейс** — кибер-панель с glassmorphism дизайном
- 📊 **Оценка рисков** — автоматическое определение уровня угрозы для артефакта

---

## 📋 Технологический стек

**Backend:**
- Python 3.11, FastAPI, Uvicorn
- TensorFlow (SavedModel), OpenAI API
- Serial communication для Arduino

**Frontend:**
- React 19, TypeScript, Vite 7
- Modern CSS (Glassmorphism, HUD elements)

**Hardware (опционально):**
- Arduino с датчиками DHT22 (температура/влажность) и LDR (освещенность)

---

## 🚀 Быстрый старт

### 1. Предварительные требования

- **Python 3.10+** (рекомендуется 3.11)
- **Node.js 18+** и npm
- **Git**
- **macOS / Linux / Windows** (WSL2 для Windows)

### 2. Клонирование репозитория

```bash
git clone https://github.com/Andasbek/robo-restorer.git
cd robo-restorer
```

### 3. Настройка окружения

**Backend:**
```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать (macOS/Linux)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

**Frontend:**
```bash
cd frontend
npm install
cd ..
```

**Конфигурация OpenAI:**
Создайте файл `.env` в корне проекта:
```env
OPENAI_API_KEY=sk-proj-ВАШ_КЛЮЧ_ЗДЕСЬ
```

### 4. Запуск проекта

**Автоматический запуск (рекомендуется):**
```bash
./run.sh
```

Скрипт автоматически запустит backend и frontend. Приложение будет доступно по адресу:
- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:8000

**Ручной запуск:**
```bash
# Terminal 1 - Backend
source venv/bin/activate
python -m backend.web_main

# Terminal 2 - Frontend
cd frontend
npm run dev
```

---

## 📱 Мобильная версия (Android / iOS)

Robo-Restorer доступен на мобильных устройствах через веб-браузер. **Никаких установок не требуется!**

**Преимущества:**
- 🚀 Мгновенный запуск - просто откройте URL в браузере
- 📱 Полный функционал - все возможности десктопной версии
- ✅ Работает на Android и iOS
- 💾 Не занимает место на телефоне

### Быстрый старт

1. **Запустите серверы на компьютере:**
   ```bash
   ./run.sh
   ```

2. **Узнайте IP адрес вашего компьютера:**
   ```bash
   # macOS
   ipconfig getifaddr en0

   # Linux
   hostname -I | awk '{print $1}'

   # Windows
   ipconfig
   ```

3. **Откройте на телефоне в браузере:**
   ```
   http://ВАШ_IP:5173
   ```

   Например: `http://10.0.98.229:5173`

**Готово!** 🎉

### Добавить на главный экран (опционально)

**Android:** Chrome → Меню (⋮) → "Добавить на главный экран"
**iOS:** Safari → Поделиться → "На экран «Домой»"

После этого приложение будет открываться как обычное приложение!

**Важно:** Убедитесь, что мобильное устройство и компьютер находятся в одной WiFi сети.

📖 **Подробная документация:** [mobile/README.md](mobile/README.md)

---

## 📖 Документация

Полная документация доступна в папке [`docs/`](docs/):

- 📘 [Обзор архитектуры](docs/ARCHITECTURE.md) — дизайн системы и взаимодействие компонентов
- 🛠️ [Установка и настройка](docs/SETUP.md) — подробное руководство по установке
- 🌐 [Справочник API](docs/API.md) — документация по эндпоинтам
- 🎭 [Сценарий демонстрации](docs/DEMO_SCENARIO.md) — скрипт для презентации

---

## 📂 Структура проекта

```
robo-restorer/
├── backend/                 # Backend сервис
│   ├── ai/                  # AI движок (TensorFlow, инференс)
│   ├── core/                # Бизнес-логика, схемы данных
│   ├── sensors/             # Работа с датчиками (Arduino/Emulator)
│   ├── ui/                  # Web API и CLI интерфейсы
│   ├── config.py            # Конфигурация
│   └── web_main.py          # Точка входа для API
├── data/                    # Данные и модели
│   ├── images/              # Тестовые изображения
│   └── models/              # TensorFlow SavedModel + labels
├── docs/                    # Документация
├── frontend/                # React + Vite интерфейс
│   ├── src/
│   │   ├── App.tsx          # Основная логика приложения
│   │   └── index.css        # Cyber-Lab дизайн
│   └── package.json
├── venv/                    # Виртуальное окружение Python
├── .env                     # Переменные окружения (OpenAI ключ)
├── requirements.txt         # Python зависимости
└── run.sh                   # Скрипт автозапуска
```

---

## 💡 Использование

### Web-интерфейс

1. Откройте http://localhost:5173 в браузере
2. Загрузите изображение артефакта (JPG, PNG, WEBP)
3. Выберите режим датчиков:
   - **Emulator Mode** — симулированные данные для демо
   - **Hardware Sensors** — реальные данные с Arduino
4. Выберите язык отчета (RU/EN)
5. Нажмите **"RUN DIAGNOSTICS"**

### Результаты анализа

- **Environmental Sensors** — показания датчиков (температура °C, влажность %, освещенность люмен)
  - *Все значения округлены до 2 знаков после запятой*
- **Visual Classification** — определенный тип повреждения и уровень уверенности
- **Risk Level** — оценка риска (LOW/MEDIUM/HIGH)
- **AI Recommendation** — конкретные рекомендации по реставрации
- **Detailed Analysis Report** — развернутый AI-отчет от OpenAI GPT-4

### CLI режим (опционально)

```bash
source venv/bin/activate
python -m backend.main --image data/images/test_artifact.jpg --emulator
```

Параметры:
- `--image` — путь к изображению артефакта
- `--emulator` — использовать эмулятор датчиков
- `--lang ru|en` — язык отчета (по умолчанию: ru)

---

## 🔧 Подключение Arduino (опционально)

Для работы с реальными датчиками:

1. Подключите Arduino с датчиками DHT22 и LDR
2. Настройте порт в `backend/sensors/serial_reader.py`:
   ```python
   SERIAL_PORT = "/dev/cu.usbmodem..."  # macOS
   # или "/dev/ttyUSB0" для Linux
   ```
3. В web-интерфейсе отключите **Emulator Mode**
4. В CLI не указывайте флаг `--emulator`

---

## ❗ Устранение неполадок

**Backend не запускается:**
- Проверьте, что виртуальное окружение активировано
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
- Проверьте совместимость версий numpy и tensorflow

**Frontend показывает "Failed to fetch":**
- Убедитесь, что backend запущен на `http://localhost:8000`
- Проверьте настройки CORS в `backend/ui/web_app.py`

**OpenAI API ошибка:**
- Проверьте файл `.env` и наличие корректного ключа
- Перезапустите backend после изменения `.env`
- Проверьте баланс и лимиты на аккаунте OpenAI

**Ошибки TensorFlow:**
- Убедитесь, что модель находится в `data/models/tm_savedmodel/`
- Проверьте наличие файла `labels.txt`
- Для macOS M1/M2 используйте `tensorflow-macos` и `tensorflow-metal`

**Конфликты зависимостей:**
- numpy: используйте версию `<2.0.0,>=1.23.5` для совместимости с TensorFlow
- opencv-python: используйте версию `<4.10` для работы с numpy<2.0

---

## 📄 Лицензия

Этот проект разработан в образовательных целях.

---

## 👥 Авторы

- Разработчик: Andasbek
- Репозиторий: https://github.com/Andasbek/robo-restorer

---

## 🙏 Благодарности

- Google Teachable Machine за упрощенное обучение моделей
- OpenAI за GPT-4 API
- Сообщество FastAPI и React за отличные фреймворки

