Ниже — черновик ТЗ именно на **часть ИИ и программного обеспечения** для проекта *AI Conservator / Robo-Restorer*. Его можно вложить в общую документацию проекта или дать как задание «разработчику ПО» (то есть вам 😉).

---

# Техническое задание

на разработку подсистемы искусственного интеллекта и программного обеспечения проекта
**«AI Conservator — хранитель древних артефактов» (+ модуль Robo-Restorer)**

---

## 1. Общая информация

**Название системы:**
AI Conservator (мониторинг артефактов) + модуль Robo-Restorer (анализ и реставрация).

**Назначение:**
Подсистема ПО и ИИ отвечает за:

1. Сбор и обработку данных от аппаратной части (Arduino + датчики).
2. Анализ изображений артефактов с помощью ИИ (классы: норма / трещина / коррозия / выцветание).
3. Принятие решений о состоянии артефакта и выдачу рекомендаций.
4. Обмен данными с модулем Robo-Restorer (в будущем — автоматический подбор раствора, запуск манипулятора).

---

## 2. Область работ (что делает разработчик ПО/ИИ)

В задачи исполнителя входит:

1. Разработка **ПК-программы** (Python), которая:

   * считывает данные с Arduino (температура, влажность, освещённость);
   * визуализирует текущие условия;
   * отображает статус (норма / плохие условия).

2. Разработка **модуля компьютерного зрения**:

   * обучение простой модели ИИ (Teachable Machine / TensorFlow) для распознавания состояния артефакта по фото;
   * интеграция модели в Python-приложение.

3. Реализация **логики принятия решений**:

   * объединение данных среды (датчики) и анализа изображения;
   * формирование текстового заключения и рекомендаций.

4. Подготовка **интерфейса пользователя**:

   * простой GUI или веб-страница/оконное приложение, на которой:

     * показываются значения датчиков;
     * отображается класс, определённый ИИ;
     * формулируются рекомендации.

5. Разработка **интерфейса интеграции с Robo-Restorer**:

   * формализованный вывод (например, JSON или текстовый протокол), который потом сможет использовать модуль Robo-Restorer (анализ сплава, выбор 5% раствора, команда роботу-манипулятору).

6. Подготовка **документации**:

   * инструкция по запуску;
   * краткое описание архитектуры;
   * примеры входных/выходных данных.

---

## 3. Общая архитектура ПО

1. **Модуль связи с Arduino**

   * Читает строки формата: `TEMP:xx.x,HUM:yy.y,LIGHT:zzz`.
   * Парсит значения, ведёт лог, передаёт наверх.

2. **Модуль мониторинга условий**

   * Сравнивает значения с порогами (напр.: влажность > 70% или температура > 30°C → “плохо”).
   * Возвращает статус: `OK / WARNING / DANGER`.

3. **Модуль ИИ-анализа изображения**

   * Принимает путь к фото артефакта.
   * Обрабатывает (resize, нормализация).
   * Отдаёт класс и вероятность.

4. **Модуль принятия решений**

   * На вход: данные датчиков + результат ИИ.
   * На выход: человекочитаемый текст, плюс структурированный объект (JSON) с полями:

     * `environment_status`
     * `image_class`
     * `risk_level`
     * `recommendation`

5. **Интерфейс пользователя**

   * Отображает:

     * числовые значения датчиков;
     * текущий статус (цвет/иконка);
     * результат ИИ и текстовое заключение.

6. **Интерфейс для Robo-Restorer**

   * REST-эндпоинт или запись в файл/очередь с результатами анализа:

     * например: `artifact_state.json`, в котором Robo-Restorer сможет прочитать, что «обнаружена коррозия бронзы, требуется очистка».

---

## 4. Функциональные требования

### 4.1. Модуль сбора данных с Arduino

* Подключение по **Serial (USB)**.
* Конфигурируемый порт и скорость (например, `COM3` / `/dev/ttyUSB0`, `9600 baud`).
* Чтение строк раз в 2 секунды (синхронно или асинхронно).
* Логирование данных в консоль и/или файл `logs/sensors.csv`.

### 4.2. Логика оценки условий

* Параметры по умолчанию:

  * Влажность: оптимум 40–60%, допустимо 30–70%, >70% — риск.
  * Температура: оптимум 18–24°C, >30°C — риск.
  * Освещённость: верхний порог для музейной витрины (например, **порог для LDR задаётся в конфиге**).
* На основе значений присваивается статус:

  * `OK` — все параметры в пределах.
  * `WARNING` — 1 параметр вышел за «комфорт», но не критично.
  * `DANGER` — 1+ критических выхода за предел.

### 4.3. Модуль ИИ-анализа изображений

* Классы:

  * `Норма`
  * `Трещина`
  * `Коррозия`
  * `Выцветание`
* Требования к данным:

  * минимум 20–30 изображений на класс для демо-уровня (можно синтетически дополнить).
* Модель:

  * Обучение в Teachable Machine или на Keras/TensorFlow.
  * Экспорт модели в формат, поддерживаемый Python (`SavedModel` или `.h5`).
* Время анализа:

  * не более 2–3 секунд на одно изображение на обычном ноутбуке.
* Вывод:

  * массив вероятностей по классам;
  * выбранный класс;
  * степень уверенности (0–1).

### 4.4. Модуль принятия решений

Примеры правил:

* Если `image_class = "Коррозия"` и `environment_status = "DANGER"` → `risk_level = "HIGH"`, рекомендация:

  > «Обнаружена коррозия и неблагоприятные условия. Требуется срочная стабилизация среды и консультация реставратора».

* Если `image_class = "Норма"` и `environment_status = "OK"` →

  > «Поверхность артефакта в норме, условия хранения соответствуют рекомендованным».

* Если `image_class = "Трещина"` →

  > «Визуально обнаружены трещины. Рекомендуется ограничить механические воздействия и оценить необходимость реставрации».

Результат должен быть доступен:

* в текстовом виде (для экрана/отчёта),
* в структурированном виде (JSON/словарь Python).

### 4.5. Интерфейс пользователя

Минимум:

* Отображение текущих значений:

  * `Температура: xx.x °C`
  * `Влажность: yy.y %`
  * `Свет: zzz`
* Цветовой индикатор статуса: зелёный / жёлтый / красный.
* Кнопка «Загрузить фото артефакта» → запуск анализа ИИ.
* Отображение результата:

  * `Класс: Коррозия (0.87)`
  * Текстовое заключение.
* (Опционально) простая историческая лента: последние N измерений.

Технологии (варианты):

* **Вариант A:** консольное приложение + простое окно (Tkinter/PyQt).
* **Вариант B:** веб-интерфейс на Flask/FastAPI + простая HTML-страница.

---

## 5. Требования к ИИ-модели

* Точность (accuracy) на тестовом наборе:

  * не ниже **80–85%** для демо-проекта.
* Возможность **переобучения**:

  * разработчик должен описать процесс обновления датасета и дообучения модели (списком шагов).
* Ограничения:

  * модель должна запускаться offline, без доступа к интернету;
  * время инициализации модели ≤ 10 секунд.

---

## 6. Нефункциональные требования

1. **Портируемость:**

   * Целевые ОС: Windows 10/11, macOS (по возможности).
2. **Простота установки:**

   * один requirements.txt или .venv-инструкция;
   * шаги запуска описаны для неподготовленного пользователя (учитель / ученик).
3. **Надёжность:**

   * программа не должна падать при временном отсутствии данных с Arduino (нужно выводить сообщение «нет связи с Arduino»).
4. **Читаемость кода:**

   * комментарии к ключевым модулям;
   * структура проекта по папкам (`/sensor`, `/ai`, `/ui`, `/config`, `/logs`).

---

## 7. Технологический стек и инструменты

**Языки и фреймворки:**

* Python 3.x
* Библиотеки:

  * `pyserial` (общение с Arduino)
  * `opencv-python` (работа с изображениями)
  * `numpy`
  * `tensorflow` / `keras` (если модель не только Teachable Machine)
  * `flask` или `fastapi` (если выбран веб-интерфейс)
  * `tkinter` / `PyQt` (если десктоп-GUI)

**Внешние инструменты:**

* Teachable Machine (обучение классификатора по фото).
* Arduino IDE (прошивка Arduino).

---

## 8. Интерфейсы и форматы данных

### 8.1. Протокол от Arduino к ПК

Одна строка раз в 2 секунды, формат:

```text
TEMP:23.4,HUM:45.6,LIGHT:512
```

Требования:

* Значения разделены запятой.
* Каждая пара ключ:значение разделена двоеточием.
* В случае некорректных данных строка игнорируется и логируется как ошибка.

### 8.2. Формат результата анализа ИИ (JSON)

Пример:

```json
{
  "image_class": "Коррозия",
  "probabilities": {
    "Норма": 0.05,
    "Трещина": 0.03,
    "Коррозия": 0.87,
    "Выцветание": 0.05
  }
}
```

### 8.3. Формат полного заключения для Robo-Restorer

```json
{
  "timestamp": "2025-11-15T10:30:00",
  "sensors": {
    "temperature": 23.4,
    "humidity": 72.1,
    "light": 430,
    "environment_status": "DANGER"
  },
  "vision": {
    "image_class": "Коррозия",
    "confidence": 0.87
  },
  "risk_level": "HIGH",
  "recommendation": "Обнаружена коррозия и неблагоприятные условия. Требуется стабилизация среды и консультация реставратора.",
  "next_action_hint": "Подготовить щадящий 5% раствор для очистки бронзы (модуль Robo-Restorer)."
}
```

---

## 9. Тестирование и приёмка

**Разработчик должен:**

1. Подготовить тестовый набор:

   * не менее 3–5 фото для каждого класса (норма, трещина, коррозия, выцветание) для демонстрации.
2. Проверить корректность:

   * чтения данных от Arduino (эмуляция через записанный лог допустима);
   * обработки датчиков (пороговые значения);
   * классификации изображений;
   * формирования итогового JSON.
3. Провести демонстрацию:

   * изменить условия (нагрев датчика, изменение света) и показать изменение статуса на экране;
   * проанализировать несколько фото артефакта и показать вывод ИИ;
   * показать файл/эндпоинт с результатами для Robo-Restorer.

**Критерий готовности:**

* ПО запускается по инструкции без доработки.
* Все основные функции (пункты 4.1–4.5) работают в демонстрационном режиме.
* Есть краткая документация (2–4 страницы/файла).

---

## 10. Документация и результаты

Разработчик ПО/ИИ передаёт:

1. Исходный код проекта (структурированный по папкам).
2. Обученную модель ИИ (файл модели + класс-обёртка в Python).
3. Файл `requirements.txt` и/или инструкцию по установке.
4. Файл с описанием архитектуры и интерфейсов (`ARCHITECTURE.md` / `README.md`).
5. Скриншоты или короткое видео работы системы (по возможности — для презентации/защиты).

---

Если хочешь, я следующим шагом могу:

* разложить это ТЗ по **этапам работы для тебя и учеников** (спринты: неделя 1, неделя 2 и т.д.),
* или сразу сделать **структуру проекта (папки + пример кода для Python-части)**, чтобы можно было начинать писать.
