# Robo-Restorer Mobile (Web Version)

> **Мобильная версия через браузер - самый простой и быстрый способ использования на телефоне**
> Версия: **v2.0.4 LAB**

Robo-Restorer доступен на мобильных устройствах через веб-браузер. Никаких установок не требуется!

## ✨ Преимущества Web-версии

- 🚀 **Мгновенный запуск** - просто откройте URL в браузере
- 📱 **Полный функционал** - все возможности десктопной версии
- 🔄 **Автоматические обновления** - всегда актуальная версия
- 💾 **Нет установки** - не занимает место на телефоне
- 🎯 **Работает на любом устройстве** - Android, iOS, планшеты

---

## 🚀 Быстрый старт

### Шаг 1: Запустите сервер на компьютере

```bash
cd /path/to/robo-restorer
./run.sh
```

Проверьте что серверы запущены:
- ✅ Backend: http://localhost:8000
- ✅ Frontend: http://localhost:5173

### Шаг 2: Узнайте IP компьютера

**macOS:**
```bash
ipconfig getifaddr en0
```

**Linux:**
```bash
hostname -I | awk '{print $1}'
```

**Windows:**
```cmd
ipconfig
```

Найдите IPv4 адрес (например: `10.0.98.229`)

### Шаг 3: Откройте на телефоне

**В браузере телефона введите:**
```
http://ВАШ_IP:5173
```

Например:
```
http://10.0.98.229:5173
```

**Готово!** 🎉

---

## 📱 Использование

### Базовый функционал

1. **Откройте браузер** на телефоне (Chrome, Safari, Firefox)
2. **Введите адрес**: `http://ВАШ_IP:5173`
3. **Загрузите изображение**:
   - Нажмите "Choose File"
   - Выберите из галереи или сделайте фото
4. **Настройте параметры**:
   - Включите/выключите Emulator Mode
   - Выберите язык (RU/EN)
5. **Запустите анализ**: нажмите "RUN DIAGNOSTICS"

### Добавить на главный экран (App-like Experience)

**Android (Chrome):**
1. Откройте сайт в Chrome
2. Нажмите меню (⋮) → "Добавить на главный экран"
3. Подтвердите
4. Теперь можно запускать как обычное приложение!

**iOS (Safari):**
1. Откройте сайт в Safari
2. Нажмите кнопку "Поделиться"
3. Выберите "На экран «Домой»"
4. Подтвердите

После этого иконка появится на главном экране, и сайт будет открываться в полноэкранном режиме!

---

## ⚙️ Требования

### На компьютере:
- ✅ Backend запущен на порту 8000
- ✅ Frontend запущен на порту 5173
- ✅ Frontend настроен для локальной сети (`host: '0.0.0.0'` в vite.config.ts)
- ✅ CORS настроен для локальной сети в backend

### На телефоне:
- ✅ Подключен к той же WiFi сети, что и компьютер
- ✅ Современный браузер (Chrome, Safari, Firefox)

---

## 🔧 Конфигурация

### Frontend (уже настроен)

**`frontend/vite.config.ts`:**
```typescript
export default defineConfig({
  plugins: [react()],
  server: {
    host: '0.0.0.0',  // Доступ из локальной сети
    port: 5173,
    strictPort: true,
  },
})
```

**`frontend/src/App.tsx`:**
```typescript
// Динамический API URL
const getApiUrl = () => {
  const apiHost = import.meta.env.VITE_API_HOST || window.location.hostname;
  return `http://${apiHost}:8000/api/analyze`;
};
const API_URL = getApiUrl();
```

### Backend CORS (уже настроен)

**`backend/ui/web_app.py`:**
```python
app.add_middleware(
    CORSMiddleware,
    allow_origin_regex=r'^http://(localhost|127\.0\.0\.1|192\.168\.\d{1,3}\.\d{1,3}|10\.\d{1,3}\.\d{1,3}\.\d{1,3}):5173$',
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## 🐛 Устранение неполадок

### Сайт не открывается

**Проблема:** `This site can't be reached`

**Решение:**
1. Проверьте что телефон и компьютер в одной WiFi сети
2. Проверьте IP адрес компьютера: `ipconfig getifaddr en0`
3. Убедитесь что frontend запущен: `curl http://localhost:5173`
4. Попробуйте отключить firewall на компьютере временно

### Кнопка "RUN DIAGNOSTICS" не работает

**Проблема:** Кнопка не отвечает или показывает ошибку

**Решение:**
1. Откройте консоль браузера (если доступно)
2. Проверьте что backend доступен: откройте `http://ВАШ_IP:8000` в браузере
3. Убедитесь что изображение выбрано
4. Проверьте подключение к WiFi

### Медленная загрузка

**Решение:**
- Это нормально при первой загрузке (кеш пустой)
- Последующие загрузки будут быстрее
- Используйте Wi-Fi 5GHz если доступен
- Убедитесь что компьютер не в режиме сна

### CORS ошибки

**Проблема:** `Access to fetch blocked by CORS policy`

**Решение:**
1. Проверьте настройки CORS в `backend/ui/web_app.py`
2. Убедитесь что ваш IP входит в разрешенный диапазон
3. Перезапустите backend

---

## 📊 Тестирование

### Проверка доступности

**На компьютере:**
```bash
# Проверка backend
curl http://localhost:8000

# Проверка frontend
curl http://localhost:5173

# Проверка из локальной сети (замените IP)
curl http://10.0.98.229:5173
```

### Тест API

```bash
curl -X POST http://10.0.98.229:8000/api/analyze \
  -F "file=@data/images/test_artifact.jpg" \
  -F "use_emulator=on" \
  -F "lang=ru"
```

---

## 📈 Производительность

### Оптимизация

**Для лучшей производительности:**
- Используйте Wi-Fi 5GHz вместо 2.4GHz
- Находитесь ближе к роутеру
- Закройте неиспользуемые вкладки браузера
- Используйте последнюю версию браузера

---

## 🔐 Безопасность

**Важно:**
- Используйте только в доверенных сетях
- Не открывайте порты в интернет без VPN
- Backend работает без аутентификации (только для локального использования)
- Для продакшн деплоя настройте HTTPS и аутентификацию

---

## 📝 Альтернатива: Native App (будущее)

Текущая папка `mobile/` содержит заготовку для React Native приложения с Expo.
Это было создано для будущего развития, но **web-версия рекомендуется** как основной способ использования на мобильных устройствах.

**Преимущества Native App:**
- Offline режим
- Push уведомления
- Более нативный UI

**Почему сейчас используем Web:**
- Быстрее и проще
- Не требует установки
- Автоматические обновления
- Работает на любой платформе

Если в будущем понадобится native app, код уже готов в этой папке.

---

## 🎯 FAQ

**Q: Нужно ли устанавливать что-то на телефон?**
A: Нет! Просто откройте в браузере.

**Q: Работает ли на iOS?**
A: Да! Работает на любом современном браузере.

**Q: Можно ли использовать вне дома?**
A: Нет, только в той же WiFi сети. Для удаленного доступа нужен VPN или облачный деплой.

**Q: Как обновить приложение?**
A: Просто перезагрузите страницу (F5 или свайп вниз в браузере).

**Q: Сохраняются ли данные?**
A: Нет, все данные обрабатываются в реальном времени без сохранения.

---

## 📚 Дополнительные ресурсы

- [Основная документация](../docs/README.md)
- [API Reference](../docs/API.md)
- [Архитектура системы](../docs/ARCHITECTURE.md)

---

## 💡 Советы

1. **Добавьте на главный экран** для быстрого доступа
2. **Используйте в горизонтальном режиме** для лучшего UX
3. **Включите полноэкранный режим** в браузере
4. **Создайте закладку** для быстрого доступа

---

**Наслаждайтесь использованием Robo-Restorer на мобильных устройствах!** 📱✨
